# Heuristics 层测试

> **不是规则，是品味。不是裁决，是提醒。**

## 层级定位

| 属性 | 值 |
|------|-----|
| **本质** | 风格/质量启发 |
| **失败策略** | ✅ 永不失败 |
| **失败含义** | 无（仅警告） |

## 职责范围

Heuristics 层负责：

1. **提供语言质量建议**
2. **提升可读性**
3. **保持文档一致性**
4. **但绝不阻断 CI**

## 核心原则

### 永远通过

```csharp
[Fact]
public void Some_Heuristic_Check()
{
    var suggestions = AnalyzeQuality();
    
    if (suggestions.Any())
    {
        // 输出建议到测试输出
        _output.WriteLine("⚠️ Heuristics 建议：...");
    }
    
    // ✅ 永远通过 - Heuristics 不应该失败构建
    Assert.True(true);
}
```

### 输出格式

```
⚠️ Heuristics 建议：以下 README 可改进语言风格

建议使用描述性语言而非命令性语言，提升文档的友好度。

  💡 docs/README.md - 建议将 '请注意' 改为描述性表达
  💡 docs/guides/getting-started.md - 建议将 '务必' 改为描述性表达

注意：这是建议，不是强制规则。
```

## 当前测试

- `DocumentationStyleHeuristicsTests.cs` - 文档风格建议
  - README 建议使用描述性语言
  - ADR 建议包含示例
  - 文档建议保持简洁

## 为什么需要这一层？

> **没有 Heuristics 的治理体系，最后一定会走向"要么放水、要么内耗"。**

**问题场景**：
- 规则太严格 → 团队绕规则
- 没有建议 → 文档质量下降
- 所有检查都阻断 → 开发体验差

**Heuristics 的价值**：
- 提供改进方向，不强制执行
- 团队可选择性采纳
- 逐步提升文档质量

## 使用 ITestOutputHelper

Heuristics 测试必须使用 `ITestOutputHelper` 输出建议：

```csharp
public class DocumentationStyleHeuristicsTests
{
    private readonly ITestOutputHelper _output;

    public DocumentationStyleHeuristicsTests(ITestOutputHelper output)
    {
        _output = output;
    }

    [Fact]
    public void Some_Heuristic_Check()
    {
        // ...
        _output.WriteLine("⚠️ Heuristics 建议：...");
    }
}
```

## CI 行为

- ❌ 不失败构建
- ⚠️ 输出到测试日志
- 📋 可选：生成 Report / Artifact

## 何时添加新测试

当需要以下内容时：

- ✅ 提供文档质量建议
- ✅ 检查可读性问题
- ✅ 建议一致性改进
- ✅ 任何"建议性"检查

**不要添加**：
- ❌ 必须修复的违规检查（属于 Enforcement）
- ❌ 治理原则验证（属于 Governance）
- ❌ 会失败构建的检查

## 与其他层的关系

```
Governance
  ↓ 定义原则
Enforcement
  ↓ 执行检查
Heuristics (这里)
  ↓ 提供建议
```

Heuristics 是三层架构的"柔性层"，避免治理过于僵化。

---

**参考**: [三层测试架构说明](../../../../docs/THREE-LAYER-TEST-ARCHITECTURE.md)
