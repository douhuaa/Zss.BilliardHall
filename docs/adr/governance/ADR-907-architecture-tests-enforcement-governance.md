---
adr: ADR-907
title: "架构测试执行治理宪章"
status: Final
level: Governance
deciders: "Architecture Board"
date: 2026-01-29
version: "1.0"
maintainer: "Architecture Board"
reviewer: "Architecture Board"
supersedes: ["ADR-903", "ADR-904", "ADR-906"]
superseded_by: null
---

# ADR-907：架构测试执行治理宪章

> ⚖️ **唯一执法权威声明**：本 ADR 是架构测试执行、分类、命名、流程的唯一裁决文件。ADR-903/904/906 已被完全吸收并取代，不再具有裁决力。所有 ArchitectureTests、Analyzer 规则、CI Gate 只允许引用 ADR-907。

**状态**：✅ Final  
**级别**：治理层  
**版本**：1.0  
**修订日期**：2026-01-29  
**适用范围**：所有架构测试、CI 流程、破例治理  
**生效时间**：即刻

---

## 聚焦内容（Focus）

- 架构测试执行级别（L1/L2/L3）唯一定义
- 架构测试组织、命名、失败消息标准
- 测试-ADR 映射与溯源机制
- CI 执行流程与阻断规则
- 破例治理与归还监控

---

## 术语表（Glossary）

| 术语        | 定义                                   | 英文对照                  |
|-----------|--------------------------------------|-------------------------|
| 架构测试      | 验证架构约束的自动化或人工检查                      | Architecture Test       |
| L1 测试     | 静态可执行自动化测试（NetArchTest）              | Level 1 Static Test     |
| L2 测试     | 语义半自动化测试（Roslyn Analyzer 或启发式）       | Level 2 Semantic Test   |
| L3 测试     | 人工审查门控（无法自动化）                        | Level 3 Manual Gate     |
| 架构违规      | 架构测试失败或未走破例流程的不合规代码                  | Architecture Violation  |
| 破例        | 已批准的临时性违规（需归还）                       | Exception / Exemption   |
| CI 阻断     | 测试失败即阻断 PR / 发布                      | CI Blocking             |
| 测试溯源      | 测试用例到 ADR 条款的双向映射                    | Test Traceability       |

---

## 核心决策（Decision）

### ADR-907.1：架构测试执行级别（唯一分级标准）

**规则**：
- 所有架构测试**必须**明确标记执行级别（L1/L2/L3）
- 执行级别**必须**基于工具能力，不得主观判断

**级别定义**：

| 级别 | 定义                        | 工具                      | 失败处理    | CI 阻断 |
|----|---------------------------|-------------------------|---------|--------|
| L1 | 静态可执行（编译期、依赖、命名）          | NetArchTest             | 绝对违规    | ✅ 强制  |
| L2 | 语义半自动（调用链、复杂度、业务逻辑）       | Roslyn Analyzer / 启发式   | 需人工审查   | ⚠️ 建议 |
| L3 | 人工门控（设计意图、业务理解）           | Code Review / ARCH-GATE | 人工决策    | ❌ 不阻断 |

**判定**：
- ✅ L1: `Modules_Should_Not_Reference_Other_Modules`（依赖检查）
- ✅ L2: `Endpoints_Should_Not_Contain_Business_Logic`（方法体复杂度）
- ✅ L3: `Handler_Should_Follow_Domain_Driven_Design`（设计原则）
- ❌ 将 L1 规则标记为 L3 以绕过 CI
- ❌ 将 L3 规则强行实现为 L1 导致误报

**【必须架构测试覆盖】**：ADR_0907_Architecture_Tests.cs → `Test_All_Architecture_Tests_Have_Enforcement_Level`

---

### ADR-907.2：架构测试组织与命名（唯一标准）

**规则**：
- 所有架构测试**必须**位于 `src/tests/ArchitectureTests/` 目录
- 测试类**必须**按 ADR 编号组织：`ADR/ADR_{XXXX}_Architecture_Tests.cs`
- 测试方法名**必须**遵循 `{Subject}_{Constraint}_{Violation_Description}` 模式

**目录结构**：
```
src/tests/ArchitectureTests/
  ADR/
    ADR_0001_Architecture_Tests.cs  # ADR-0001 的所有测试
    ADR_0002_Architecture_Tests.cs  # ADR-0002 的所有测试
    ADR_0907_Architecture_Tests.cs  # 本 ADR 的自检测试
  Enforcement/                       # 通用执行工具
  Governance/                        # 治理层测试
  Heuristics/                        # 启发式检查
```

**命名规则**：
- ✅ `Modules_Should_Not_Reference_Other_Modules`
- ✅ `Handlers_Should_Not_Depend_On_AspNet`
- ✅ `CommandHandlers_Should_Not_Return_Business_Data`
- ❌ `Test1` / `CheckModules`（不清晰）
- ❌ `VerifyNoCircularDependency`（缺少主体）
- ❌ `TestHandlerNaming`（缺少约束描述）

**【必须架构测试覆盖】**：ADR_0907_Architecture_Tests.cs → `Test_All_Architecture_Test_Classes_Follow_Naming_Convention`

---

### ADR-907.3：测试失败消息标准（唯一格式）

**规则**：
- 测试失败消息**必须**包含：ADR 编号、违规描述、修复建议、文档链接
- 格式**必须**为：`❌ ADR-XXXX.Y 违规: {原因}。\n修复建议：{建议}\n参考：{ADR路径}`

**消息模板**：
```csharp
Assert.True(result.IsSuccessful,
    $"❌ ADR-{adrNumber}.{ruleNumber} 违规: {violation}。\n" +
    $"违规类型: {failingTypes}。\n" +
    $"修复建议：\n" +
    $"  1. {suggestion1}\n" +
    $"  2. {suggestion2}\n" +
    $"参考：docs/adr/{category}/ADR-{adrNumber}-{filename}.md");
```

**判定**：
- ✅ 包含 ADR 编号、违规类型、修复建议、文档链接
- ✅ 使用 ❌ 表情符号标记违规
- ❌ 仅显示 "Test failed"
- ❌ 缺少修复建议
- ❌ 缺少 ADR 文档链接

**【必须架构测试覆盖】**：ADR_0907_Architecture_Tests.cs → `Test_All_Architecture_Tests_Have_Proper_Failure_Messages`

---

### ADR-907.4：测试-ADR 映射与溯源（强制双向）

**规则**：
- 每个标记【必须架构测试覆盖】的 ADR 条款**必须**至少有一个对应测试
- 每个架构测试**必须**在失败消息中引用对应的 ADR 条款
- ADR 文档**必须**在"执法模型"章节列出所有对应测试

**映射格式（ADR 文档）**：
```markdown
## 执法模型（Enforcement）

| 规则编号       | 执行级 | 测试方法                                  |
|------------|-----|---------------------------------------|
| ADR-0001.1 | L1  | Modules_Should_Not_Reference_Other_Modules |
| ADR-0001.2 | L1  | Module_Csproj_Should_Not_Reference_Other_Modules |
```

**判定**：
- ✅ ADR 标记【必须架构测试覆盖】→ 存在对应测试类
- ✅ 测试失败消息引用 ADR-XXXX.Y
- ❌ ADR 标记测试覆盖但无对应测试
- ❌ 测试存在但 ADR 未记录
- ❌ 测试失败消息不包含 ADR 编号

**【必须架构测试覆盖】**：ADR_0907_Architecture_Tests.cs → `Test_ADR_Test_Mapping_Is_Complete`

---

### ADR-907.5：CI 执行流程与阻断规则（唯一流程）

**规则**：
- 所有 L1 测试失败**必须**阻断 PR 合并
- L2 测试失败**应当**触发人工审查，但不阻断 CI
- L3 检查项**必须**在 PR 模板中列出，需人工确认

**CI 流程**：
```yaml
# .github/workflows/architecture-tests.yml
steps:
  1. 运行所有 L1 测试（NetArchTest）
     - 失败 → CI 失败 → PR 阻断
  2. 运行所有 L2 测试（Roslyn Analyzer）
     - 失败 → 警告日志 → 需人工审查
  3. 检查 L3 检查清单
     - 未填写 → PR 阻断
```

**判定**：
- ✅ L1 测试失败 → CI 返回非零退出码
- ✅ L2 测试失败 → 生成警告但 CI 通过
- ✅ L3 清单必填项未勾选 → PR 阻断
- ❌ L1 测试失败但 CI 通过
- ❌ 直接跳过架构测试
- ❌ 绕过 CI 直接合并

**【必须架构测试覆盖】**：ADR_0907_Architecture_Tests.cs → `Test_All_L1_Tests_Are_Enforced_By_CI`

---

### ADR-907.6：破例治理与归还监控（强制溯源）

**规则**：
- 所有架构破例**必须**记录在 `ARCH-VIOLATIONS.md`
- 每个破例**必须**包含：ADR 编号、到期版本、负责人、归还计划
- CI **必须**自动扫描过期破例并失败构建

**破例记录格式**：
```markdown
## arch-violations.md

| ADR         | 规则 | 违规文件 | 到期版本 | 负责人 | 归还计划 | 状态 |
|-------------|------|---------|---------|--------|---------|------|
| ADR-0001.1  | 模块隔离 | Orders/Legacy.cs | v2.5.0 | @dev | 迁移至新模块 | 🚧 进行中 |
```

**CI 监控**：
```bash
# scripts/check-arch-violations.sh
# 每次构建执行，检查：
1. 所有破例是否有到期版本
2. 当前版本是否已超过到期版本
3. 过期破例 → 构建失败
```

**判定**：
- ✅ 破例记录包含所有必填项
- ✅ CI 扫描发现过期破例 → 构建失败
- ❌ 破例未记录在 ARCH-VIOLATIONS.md
- ❌ 破例无到期版本或归还计划
- ❌ 过期破例存在但 CI 未阻断

**【必须架构测试覆盖】**：ADR_0907_Architecture_Tests.cs → `Test_Arch_Violations_File_Exists_And_Format_Is_Valid`

---

### ADR-907.7：归档 ADR 不得参与执法（排除规则）

**规则**：
- 所有位于 `docs/adr/archive/` 的 ADR **禁止**生成架构测试
- 所有状态为 `Superseded` 的 ADR **禁止**被 Analyzer / CI 读取
- Analyzer / Script **必须**显式排除 `archive/` 目录

**排除机制**：
```csharp
// 扫描 ADR 时必须排除
var adrFiles = Directory.GetFiles("docs/adr", "*.md", SearchOption.AllDirectories)
    .Where(f => !f.Contains("/archive/"))
    .Where(f => !IsSuperseded(f));
```

**判定**：
- ✅ archive/ 目录中的 ADR 不生成 RuleId
- ✅ Superseded 状态的 ADR 不被 CI 读取
- ✅ 测试扫描脚本显式排除 archive/
- ❌ Superseded ADR 仍被用于生成测试
- ❌ archive/ 中的 ADR 仍被 Analyzer 读取
- ❌ 脚本未排除归档 ADR

**【必须架构测试覆盖】**：ADR_0907_Architecture_Tests.cs → `Test_Archive_And_Superseded_ADRs_Are_Excluded_From_Enforcement`

---

## 执法模型（Enforcement）

### 测试映射

| 规则编号       | 执行级 | 测试方法                                                     |
|------------|-----|----------------------------------------------------------|
| ADR-907.1  | L1  | Test_All_Architecture_Tests_Have_Enforcement_Level       |
| ADR-907.2  | L1  | Test_All_Architecture_Test_Classes_Follow_Naming_Convention |
| ADR-907.3  | L2  | Test_All_Architecture_Tests_Have_Proper_Failure_Messages |
| ADR-907.4  | L1  | Test_ADR_Test_Mapping_Is_Complete                        |
| ADR-907.5  | L1  | Test_All_L1_Tests_Are_Enforced_By_CI                     |
| ADR-907.6  | L1  | Test_Arch_Violations_File_Exists_And_Format_Is_Valid     |
| ADR-907.7  | L1  | Test_Archive_And_Superseded_ADRs_Are_Excluded_From_Enforcement |

---

## 破例与归还（Exception）

### 允许破例的前提

破例**仅在以下情况允许**：
- 遗留代码迁移期（需明确迁移计划）
- 工具能力限制（需提供技术论证）
- 性能关键路径（需性能测试数据支持）

### 破例要求（不可省略）

每个破例**必须**：
- 记录在 `ARCH-VIOLATIONS.md`
- 指明 ADR 编号 + 规则编号
- 指定到期版本号（如 v2.5.0）
- 给出归还计划和负责人
- 获得架构委员会批准

**未记录的破例 = 未授权架构违规。**

---

## 变更政策（Change Policy）

### 变更规则

本 ADR 为 **Final** 状态：
- 修改需架构委员会全体批准
- 需全量回归测试
- 需更新所有依赖此 ADR 的文档和测试

### 失效与替代

本 ADR 完全取代 ADR-903/904/906：
- ADR-903/904/906 已移至 `docs/adr/archive/governance/`
- 不得单独引用 ADR-903/904/906
- 所有引用必须指向 ADR-907

---

## 明确不管什么（Non-Goals）

本 ADR **不负责**：
- 单元测试组织（参见 ADR-122）
- 集成测试策略
- 代码覆盖率要求
- 性能测试标准
- 具体业务领域测试

---

## 关系（Relationships）

### 取代（Supersedes）

- [ADR-903](../archive/governance/ADR-903-architecture-tests-classification.md) - 架构测试分类标准（已归档）
- [ADR-904](../archive/governance/ADR-904-architecture-tests-naming.md) - 架构测试命名规范（已归档）
- [ADR-906](../archive/governance/ADR-906-architecture-tests-execution.md) - 架构测试执行流程（已归档）

> ⚠️ **治理级宣告**：ADR-903 / 904 / 906 不得被单独引用。所有 ArchitectureTests、Analyzer 规则、CI Gate 只允许引用 ADR-907。

### 依赖（Depends On）

- [ADR-0000](./ADR-0000-architecture-tests.md) - 架构测试与 CI 治理宪法
- [ADR-905](./ADR-905-enforcement-level-classification.md) - 执行级别分类（补充）
- [ADR-122](../structure/ADR-122-test-organization-naming.md) - 测试代码组织与命名规范

### 扩展（Extended By）

- [ADR-905](./ADR-905-enforcement-level-classification.md) - 详细的执行级别定义和工具指南

### 参考（References）

- [ADR-900](./ADR-900-adr-process.md) - ADR 新增与修订流程
- [ADR-930](./ADR-930-code-review-compliance.md) - 代码评审合规性

---

## 版本历史（Version History）

| 版本  | 日期         | 变更说明                                      | 作者               |
|-----|------------|-------------------------------------------|--------------------|
| 1.0 | 2026-01-29 | 初始版本，合并 ADR-903/904/906，建立唯一执法入口 | Architecture Board |

---

**维护者**：Architecture Board  
**审核者**：Architecture Board  
**最后更新**：2026-01-29
