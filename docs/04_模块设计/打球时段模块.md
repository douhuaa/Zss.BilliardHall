---
title: "打球时段模块（Sessions）"
description: "基于 Wolverine 垂直切片架构的打球时段管理模块设计"
version: "2.0.0"
author: "架构团队"
maintainer: "后端开发团队"
created: "2024-01-15"
updated: "2024-01-15"
category: "模块设计"
level: "核心"
audience: ["后端开发工程师", "业务分析师"]
keywords: ["Sessions", "时段管理", "TableSessionSaga", "Wolverine", "垂直切片"]
tags: ["sessions", "timing", "saga", "vertical-slice", "wolverine"]
dependencies: ["系统架构设计", "Wolverine模块化架构蓝图"]
estimated_reading_time: "15分钟"
architecture: "Wolverine + 垂直切片"
---

# 打球时段模块（Sessions）

<!-- Breadcrumb Navigation -->
**导航路径**: [🏠 项目文档](../../README.md) > [📦 模块设计](README.md) > ⏱️ 打球时段模块

<!-- Keywords for Search -->
**关键词**: `Sessions` `时段管理` `TableSessionSaga` `Wolverine` `垂直切片`

---

## 📋 模块概述

打球时段模块（Sessions）负责管理桌台的使用时段，包括时段的开始、暂停、恢复、延长和结束，以及时长的计算。采用 **Wolverine + 垂直切片架构**，通过 TableSessionSaga 管理时段的完整生命周期。

### 核心职责

- 时段生命周期管理（开始、暂停、恢复、结束）
- 暂停记录与时长计算
- 时段状态跟踪
- 跨模块事件发布（SessionStarted、SessionEnded 等）

### 架构参考

> 📚 **完整架构实现**: [Wolverine模块化架构蓝图](../03_系统架构设计/Wolverine模块化架构蓝图.md)
> 
> 📚 **模块边界定义**: [系统模块划分 - Sessions 模块](../03_系统架构设计/系统模块划分.md#4-sessions-模块打球时段)

---

## 🏗️ 模块结构

### 目录组织

```
src/Modules/Sessions/
├── StartSession/
│   ├── StartSession.cs                # Command
│   ├── StartSessionHandler.cs         # Handler
│   ├── StartSessionEndpoint.cs        # HTTP Endpoint
│   └── StartSessionValidator.cs       # Validator
│
├── EndSession/
│   ├── EndSession.cs
│   ├── EndSessionHandler.cs
│   └── EndSessionEndpoint.cs
│
├── PauseSession/
│   ├── PauseSession.cs
│   ├── PauseSessionHandler.cs
│   └── PauseSessionEndpoint.cs
│
├── ResumeSession/
│   ├── ResumeSession.cs
│   ├── ResumeSessionHandler.cs
│   └── ResumeSessionEndpoint.cs
│
├── ExtendSession/
│   ├── ExtendSession.cs
│   ├── ExtendSessionHandler.cs
│   └── ExtendSessionEndpoint.cs
│
├── GetSession/
│   ├── GetSession.cs                  # Query
│   ├── GetSessionHandler.cs
│   └── GetSessionEndpoint.cs
│
├── GetActiveSessionByTable/
│   ├── GetActiveSessionByTable.cs
│   └── GetActiveSessionByTableHandler.cs
│
├── GetSessionHistory/
│   ├── GetSessionHistory.cs
│   └── GetSessionHistoryHandler.cs
│
├── Events/
│   ├── SessionStarted.cs              # 时段已开始
│   ├── SessionEnded.cs                # 时段已结束
│   ├── SessionPaused.cs               # 时段已暂停
│   └── SessionResumed.cs              # 时段已恢复
│
├── Sagas/
│   └── TableSessionSaga.cs            # 时段生命周期管理
│
├── TableSession.cs                     # 聚合根
├── PauseRecord.cs                      # 值对象：暂停记录
├── SessionStatus.cs                    # 枚举
└── SessionsModule.cs                   # 模块标记
```

---

## 💻 功能切片

### 1. StartSession（开始时段）

#### Command 定义

```csharp
public sealed record StartSession(
    Guid TableId,
    Guid? MemberId,           // 会员ID（散客为null）
    string? CustomerName      // 散客姓名（可选）
);
```

#### Handler 实现

```csharp
public sealed class StartSessionHandler
{
    [Transactional]
    public async Task<Result<Guid>> Handle(
        StartSession command,
        IDocumentSession session,
        IMessageBus bus,
        CancellationToken ct = default)
    {
        // 1. 验证桌台是否可用
        var table = await session.LoadAsync<Table>(command.TableId, ct);
        if (table == null)
            return Result.Fail<Guid>("Tables:TableNotFound", "桌台不存在");
        
        if (table.Status != TableStatus.Available)
            return Result.Fail<Guid>("Tables:TableUnavailable", "桌台不可用");
        
        // 2. 检查是否已有活跃时段
        var existingSession = await session.Query<TableSession>()
            .FirstOrDefaultAsync(s => s.TableId == command.TableId && 
                                     (s.Status == SessionStatus.Active || s.Status == SessionStatus.Paused), ct);
        
        if (existingSession != null)
            return Result.Fail<Guid>("Sessions:SessionAlreadyActive", "该桌台已有活跃时段");
        
        // 3. 创建新时段
        var tableSession = new TableSession
        {
            Id = Guid.NewGuid(),
            TableId = command.TableId,
            MemberId = command.MemberId,
            CustomerName = command.CustomerName,
            Status = SessionStatus.Active,
            StartTime = DateTimeOffset.UtcNow,
            PauseRecords = new List<PauseRecord>(),
            CreatedAt = DateTimeOffset.UtcNow
        };
        
        session.Store(tableSession);
        await session.SaveChangesAsync(ct);
        
        // 4. 发布事件
        await bus.PublishAsync(new SessionStarted(
            tableSession.Id,
            command.TableId,
            command.MemberId,
            tableSession.StartTime
        ), ct);
        
        // 5. 发送命令到 Tables 模块标记桌台为占用
        await bus.InvokeAsync(new ReserveTable(command.TableId, tableSession.Id), ct);
        
        return Result.Ok(tableSession.Id);
    }
}
```

#### HTTP Endpoint

```csharp
public sealed class StartSessionEndpoint
{
    [WolverinePost("/api/sessions/start")]
    public static StartSession Post(StartSessionRequest request)
        => new(request.TableId, request.MemberId, request.CustomerName);
}

public sealed record StartSessionRequest(
    Guid TableId,
    Guid? MemberId,
    string? CustomerName
);
```

#### Validator

```csharp
public sealed class StartSessionValidator : AbstractValidator<StartSession>
{
    public StartSessionValidator()
    {
        RuleFor(x => x.TableId)
            .NotEmpty().WithMessage("TableId 不能为空");
        
        When(x => x.MemberId == null, () =>
        {
            RuleFor(x => x.CustomerName)
                .NotEmpty().WithMessage("散客必须提供姓名")
                .MaximumLength(50).WithMessage("姓名不能超过50字符");
        });
    }
}
```

---

### 2. EndSession（结束时段）

#### Command 定义

```csharp
public sealed record EndSession(Guid SessionId);
```

#### Handler 实现

```csharp
public sealed class EndSessionHandler
{
    [Transactional]
    public async Task<Result> Handle(
        EndSession command,
        IDocumentSession session,
        IMessageBus bus,
        CancellationToken ct = default)
    {
        var tableSession = await session.LoadAsync<TableSession>(command.SessionId, ct);
        if (tableSession == null)
            return Result.Fail("Sessions:SessionNotFound", "时段不存在");
        
        if (tableSession.Status == SessionStatus.Ended || tableSession.Status == SessionStatus.Completed)
            return Result.Fail("Sessions:SessionAlreadyEnded", "时段已结束");
        
        // 计算总时长（扣除暂停时间）
        tableSession.EndTime = DateTimeOffset.UtcNow;
        tableSession.Status = SessionStatus.Ended;
        tableSession.CalculatedDuration = CalculateBillableDuration(tableSession);
        
        session.Store(tableSession);
        await session.SaveChangesAsync(ct);
        
        // 发布事件
        await bus.PublishAsync(new SessionEnded(
            tableSession.Id,
            tableSession.TableId,
            tableSession.CalculatedDuration
        ), ct);
        
        // 发送命令到 Tables 模块释放桌台
        await bus.InvokeAsync(new ReleaseTable(tableSession.TableId), ct);
        
        return Result.Ok();
    }
    
    private TimeSpan CalculateBillableDuration(TableSession session)
    {
        var totalDuration = session.EndTime!.Value - session.StartTime;
        var pausedDuration = session.PauseRecords
            .Where(p => p.ResumedAt.HasValue)
            .Sum(p => (p.ResumedAt!.Value - p.PausedAt).TotalMilliseconds);
        
        return totalDuration - TimeSpan.FromMilliseconds(pausedDuration);
    }
}
```

---

### 3. PauseSession / ResumeSession（暂停/恢复）

#### Command 定义

```csharp
public sealed record PauseSession(Guid SessionId, string? Reason);
public sealed record ResumeSession(Guid SessionId);
```

#### PauseSessionHandler

```csharp
public sealed class PauseSessionHandler
{
    [Transactional]
    public async Task<Result> Handle(
        PauseSession command,
        IDocumentSession session,
        IMessageBus bus,
        CancellationToken ct = default)
    {
        var tableSession = await session.LoadAsync<TableSession>(command.SessionId, ct);
        if (tableSession == null)
            return Result.Fail("Sessions:SessionNotFound", "时段不存在");
        
        if (tableSession.Status != SessionStatus.Active)
            return Result.Fail("Sessions:SessionNotActive", "只能暂停进行中的时段");
        
        tableSession.Status = SessionStatus.Paused;
        tableSession.PauseRecords.Add(new PauseRecord
        {
            PausedAt = DateTimeOffset.UtcNow,
            Reason = command.Reason
        });
        
        session.Store(tableSession);
        await session.SaveChangesAsync(ct);
        
        await bus.PublishAsync(new SessionPaused(tableSession.Id, tableSession.TableId), ct);
        
        return Result.Ok();
    }
}
```

#### ResumeSessionHandler

```csharp
public sealed class ResumeSessionHandler
{
    [Transactional]
    public async Task<Result> Handle(
        ResumeSession command,
        IDocumentSession session,
        IMessageBus bus,
        CancellationToken ct = default)
    {
        var tableSession = await session.LoadAsync<TableSession>(command.SessionId, ct);
        if (tableSession == null)
            return Result.Fail("Sessions:SessionNotFound", "时段不存在");
        
        if (tableSession.Status != SessionStatus.Paused)
            return Result.Fail("Sessions:SessionNotPaused", "只能恢复已暂停的时段");
        
        var lastPause = tableSession.PauseRecords.LastOrDefault(p => !p.ResumedAt.HasValue);
        if (lastPause != null)
            lastPause.ResumedAt = DateTimeOffset.UtcNow;
        
        tableSession.Status = SessionStatus.Active;
        
        session.Store(tableSession);
        await session.SaveChangesAsync(ct);
        
        await bus.PublishAsync(new SessionResumed(tableSession.Id, tableSession.TableId), ct);
        
        return Result.Ok();
    }
}
```

---

### 4. ExtendSession（延长时段）

#### Command 定义

```csharp
public sealed record ExtendSession(
    Guid SessionId,
    TimeSpan ExtensionDuration
);
```

#### Handler 实现

```csharp
public sealed class ExtendSessionHandler
{
    [Transactional]
    public async Task<Result> Handle(
        ExtendSession command,
        IDocumentSession session,
        CancellationToken ct = default)
    {
        var tableSession = await session.LoadAsync<TableSession>(command.SessionId, ct);
        if (tableSession == null)
            return Result.Fail("Sessions:SessionNotFound", "时段不存在");
        
        if (tableSession.Status != SessionStatus.Active && tableSession.Status != SessionStatus.Paused)
            return Result.Fail("Sessions:CannotExtend", "只能延长进行中或暂停中的时段");
        
        // 延长逻辑可以根据业务需求实现
        // 例如：记录延长时间、通知计费模块等
        
        return Result.Ok();
    }
}
```

---

### 5. GetSession（查询时段）

#### Query 定义

```csharp
public sealed record GetSession(Guid SessionId);
```

#### Handler 实现

```csharp
public sealed class GetSessionHandler
{
    public async Task<TableSession?> Handle(
        GetSession query,
        IDocumentSession session,
        CancellationToken ct = default)
    {
        return await session.LoadAsync<TableSession>(query.SessionId, ct);
    }
}
```

---

## 📊 领域模型

### TableSession（聚合根）

```csharp
public class TableSession
{
    public Guid Id { get; set; }
    public Guid TableId { get; set; }
    public Guid? MemberId { get; set; }
    public string? CustomerName { get; set; }       // 散客姓名
    public SessionStatus Status { get; set; }
    public DateTimeOffset StartTime { get; set; }
    public DateTimeOffset? EndTime { get; set; }
    public List<PauseRecord> PauseRecords { get; set; } = new();
    public TimeSpan CalculatedDuration { get; set; }
    public DateTimeOffset CreatedAt { get; set; }
}
```

### PauseRecord（值对象）

```csharp
public class PauseRecord
{
    public DateTimeOffset PausedAt { get; set; }
    public DateTimeOffset? ResumedAt { get; set; }
    public string? Reason { get; set; }
}
```

### SessionStatus（枚举）

```csharp
public enum SessionStatus
{
    Active,         // 进行中
    Paused,         // 暂停中
    Ended,          // 已结束（等待支付）
    Completed       // 已完成（已支付）
}
```

---

## 🔔 领域事件

```csharp
public sealed record SessionStarted(
    Guid SessionId,
    Guid TableId,
    Guid? MemberId,
    DateTimeOffset StartTime
);

public sealed record SessionEnded(
    Guid SessionId,
    Guid TableId,
    TimeSpan Duration
);

public sealed record SessionPaused(
    Guid SessionId,
    Guid TableId
);

public sealed record SessionResumed(
    Guid SessionId,
    Guid TableId
);
```

---

## 🔗 跨模块通信

### 发送命令

- → `ReserveTable` (到 Tables 模块) - 开始时段时标记桌台占用
- → `ReleaseTable` (到 Tables 模块) - 结束时段时释放桌台

### 发布事件

- `SessionStarted` → Devices 模块监听（自动开灯/开门）
- `SessionEnded` → Billing 模块监听（自动计算账单）

### 监听事件

- `PaymentCompleted` (来自 Payments 模块) → 标记时段为已完成

---

## 🎯 TableSessionSaga（时段生命周期管理）

```csharp
public sealed class TableSessionSaga : Saga
{
    public Guid SessionId { get; set; }
    public Guid TableId { get; set; }
    public Guid? BillId { get; set; }
    public SessionStatus Status { get; set; }
    
    // 时段开始
    public void Handle(SessionStarted @event)
    {
        SessionId = @event.SessionId;
        TableId = @event.TableId;
        Status = SessionStatus.Active;
    }
    
    // 时段结束 → 等待账单计算
    public void Handle(SessionEnded @event)
    {
        Status = SessionStatus.Ended;
    }
    
    // 账单计算完成 → 等待支付
    public void Handle(BillCalculated @event)
    {
        BillId = @event.BillId;
    }
    
    // 支付完成 → 时段完成
    public void Handle(PaymentCompleted @event)
    {
        if (@event.SessionId == SessionId)
        {
            Status = SessionStatus.Completed;
            Complete();  // 完成 Saga
        }
    }
}
```

---

## 🧪 单元测试示例

### StartSessionHandler 测试

```csharp
public class StartSessionHandlerTests
{
    [Fact]
    public async Task Handle_ValidTable_StartsSession()
    {
        // Arrange
        var store = DocumentStore.For(opts =>
        {
            opts.Connection("connection string");
            opts.DatabaseSchemaName = "sessions_test";
        });
        
        await using var session = store.LightweightSession();
        var bus = Substitute.For<IMessageBus>();
        
        var table = new Table
        {
            Id = Guid.NewGuid(),
            Name = "1号桌",
            Type = TableType.Chinese,
            Status = TableStatus.Available,
            HourlyRate = 30m
        };
        session.Store(table);
        await session.SaveChangesAsync();
        
        var handler = new StartSessionHandler();
        var command = new StartSession(table.Id, null, "张三");
        
        // Act
        var result = await handler.Handle(command, session, bus, CancellationToken.None);
        
        // Assert
        Assert.True(result.IsSuccess);
        Assert.NotEqual(Guid.Empty, result.Value);
        
        var createdSession = await session.LoadAsync<TableSession>(result.Value);
        Assert.NotNull(createdSession);
        Assert.Equal(table.Id, createdSession.TableId);
        Assert.Equal(SessionStatus.Active, createdSession.Status);
        
        await bus.Received(1).PublishAsync(
            Arg.Is<SessionStarted>(e => e.SessionId == result.Value),
            Arg.Any<CancellationToken>()
        );
    }
    
    [Fact]
    public async Task Handle_TableOccupied_ReturnsFailure()
    {
        // Arrange
        var store = DocumentStore.For(opts =>
        {
            opts.Connection("connection string");
            opts.DatabaseSchemaName = "sessions_test";
        });
        
        await using var session = store.LightweightSession();
        var bus = Substitute.For<IMessageBus>();
        
        var table = new Table
        {
            Id = Guid.NewGuid(),
            Name = "1号桌",
            Status = TableStatus.Occupied
        };
        session.Store(table);
        await session.SaveChangesAsync();
        
        var handler = new StartSessionHandler();
        var command = new StartSession(table.Id, null, "李四");
        
        // Act
        var result = await handler.Handle(command, session, bus, CancellationToken.None);
        
        // Assert
        Assert.False(result.IsSuccess);
        Assert.Equal("Tables:TableUnavailable", result.Error?.Code);
    }
}
```

---

## 📝 开发规范

### 命名约定

- **Command**: 动词开头，描述操作（如 `StartSession`, `EndSession`）
- **Query**: `Get` 开头（如 `GetSession`）
- **Event**: 过去式，描述事实（如 `SessionStarted`, `SessionEnded`）
- **Handler**: `{CommandName}Handler`
- **Endpoint**: `{CommandName}Endpoint`

### 验证规则

- 所有公开命令必须有 Validator
- 业务规则验证在 Handler 中
- 使用 FluentValidation 进行输入验证

### 日志规范

```csharp
_logger.LogInformation(
    "Session {SessionId} started for Table {TableId} by {Customer}",
    sessionId, tableId, customerName ?? $"Member:{memberId}"
);

_logger.LogWarning(
    "Failed to start session for Table {TableId}: {Reason}",
    tableId, errorReason
);
```

### 错误处理

- 使用 Result 模式返回错误
- 错误代码格式：`Sessions:{ErrorKey}`
- 示例：`Sessions:SessionNotFound`, `Sessions:SessionAlreadyActive`

---

## 📚 相关文档

- [Wolverine模块化架构蓝图](../03_系统架构设计/Wolverine模块化架构蓝图.md)
- [系统模块划分](../03_系统架构设计/系统模块划分.md)
- [Wolverine快速上手指南](../03_系统架构设计/Wolverine快速上手指南.md)
- [计费管理模块](计费管理模块.md)（相关模块）
- [会员管理模块](会员管理模块.md)

---

*最后更新: 2024-01-15 | 版本: v2.0.0 | 架构: Wolverine + 垂直切片*
