# ADR 自动化工具使用指南

> ⚠️ **无裁决力声明**：本文档仅供参考，不具备架构裁决权。
> 所有架构决策以相关 ADR 正文为准。详见 [ADR 目录](adr/README.md)。

本文档提供 ADR 治理体系自动化工具的详细使用指南，帮助团队成员高效管理和维护 ADR 文档。

---

## 目录

- [快速开始](#快速开始)
- [工具详解](#工具详解)
- [常见场景](#常见场景)
- [最佳实践](#最佳实践)
- [故障排查](#故障排查)

---

## 快速开始

### 新成员上手

1. **了解工具**
   ```bash
   cd scripts
   ls -la *.sh
   ```

2. **运行健康检查**
   ```bash
   ./scripts/validate-adr-consistency.sh
   ./scripts/validate-three-way-mapping.sh
   ```

3. **查看工具帮助**
   ```bash
   ./scripts/adr-cli.sh help
   ```

### 日常验证

在提交 PR 前，运行以下命令确保一致性：

```bash
# 方式 1：使用 ADR CLI 统一验证
./scripts/adr-cli.sh validate

# 方式 2：单独运行各工具
./scripts/validate-adr-consistency.sh
./scripts/validate-three-way-mapping.sh
```

---

## 工具详解

### 1. ADR 一致性检查器

#### 用途

验证 ADR 文档的结构完整性，确保编号、目录和内容的三元一致。

#### 检查项目

| 检查项 | 说明 | 示例 |
|-------|------|------|
| 编号格式 | 必须为4位数字 | `0001` ✅ vs `1` ❌ |
| 目录层级 | 编号须在层级范围内 | `0150` 在 `structure/` ✅ |
| 元数据 | 状态、级别字段完整 | **状态**：Final ✅ |
| 文件命名 | 符合命名规范 | `ADR-0001-title.md` ✅ |
| 编号连续 | 检测跳号 | 0001→0003 ⚠️ 跳过 0002 |

#### 使用示例

```bash
# 基本使用
./scripts/validate-adr-consistency.sh

# 查看详细输出
./scripts/validate-adr-consistency.sh | tee adr-check.log
```

#### 输出解读

```
✅ 编号格式正确：0001
✅ 目录位置正确：constitutional (范围: 0001-0099)
✅ 元数据完整
✅ 文件命名符合规范
```

#### 常见错误

**错误：编号格式不正确**
```
❌ 编号格式错误：应为4位数字（如 0001），当前为 1
```
解决：重命名文件使用4位编号：
```bash
mv docs/adr/constitutional/ADR-1-*.md docs/adr/constitutional/ADR-0001-*.md
```

**错误：目录层级错误**
```
❌ 目录位置错误：ADR-0150 不在 constitutional 的编号范围内
```
解决：移动到正确目录：
```bash
mv docs/adr/constitutional/ADR-0150-*.md docs/adr/structure/
```

---

### 2. 三位一体映射验证器

#### 用途

验证 ADR 文档、架构测试和 Copilot Prompts 三者的映射关系，确保治理体系的完整性。

#### 检查逻辑

```
ADR 文档 ←→ 架构测试
    ↓
    ↓
    ↓
Copilot Prompts
```

#### 映射规则

1. 带架构测试标记的 ADR → 对应测试文件
2. 所有 ADR → 建议有 Prompt 文件
3. 测试文件 → 对应 ADR
4. Prompt 文件 → 对应 ADR

#### 使用示例

```bash
# 基本使用
./scripts/validate-three-way-mapping.sh

# 保存报告
./scripts/validate-three-way-mapping.sh > mapping-report.txt
```

#### 健康度指标

| 指标 | 标准 | 说明 |
|-----|------|------|
| 测试覆盖率 | 100% | 所有需要测试覆盖的 ADR 都会有测试 |
| Prompt 映射率 | ≥80% | 大部分 ADR 都有 Prompt |
| 孤立文件 | 0 | 无孤立的测试或 Prompt 文件 |

#### 修正清单

工具会自动生成修正清单：

```
📋 修正清单

需要添加测试文件：
  [ ] 为 ADR-0001 创建测试文件：
      src/tests/ArchitectureTests/ADR/ADR_0001_Architecture_Tests.cs
      参考：docs/adr/governance/ADR-0000-architecture-tests.md

需要添加 Prompt 文件：
  [ ] 为 ADR-0001 创建 Prompt 文件：
      docs/copilot/adr-1.prompts.md
      参考：docs/templates/copilot-prompts-template.md
```

---

### 3. ADR 管理 CLI

#### 用途

提供统一的命令行界面，简化 ADR 的创建和管理流程。

#### 命令概览

```bash
adr-cli.sh create <tier> <title>      # 创建新 ADR
adr-cli.sh next-number <tier>         # 查询下一个可用编号
adr-cli.sh list [tier]                # 列出 ADR
adr-cli.sh validate                   # 运行所有验证
```

#### 创建 ADR 详细流程

```bash
# 1. 创建新 ADR
./scripts/adr-cli.sh create structure "领域事件命名规范"

# 工具自动执行：
# ✅ 分配编号：ADR-0120
# ✅ 创建 ADR 文档：docs/adr/structure/ADR-0120-domain-event-naming.md
# ✅ 创建 Prompt 文件：docs/copilot/adr-120.prompts.md
# ℹ️  提示创建测试文件：src/tests/ArchitectureTests/ADR/ADR_0120_Architecture_Tests.cs
```

#### 层级选择指南

| 层级 | 编号 | 适用场景 | 示例 |
|-----|------|---------|------|
| constitutional | 0001-0099 | 系统基础约束 | 模块隔离、依赖规则 |
| structure | 0100-0199 | 代码组织命名 | 命名规范、文件结构 |
| runtime | 0200-0299 | 运行时行为 | 生命周期、异常处理 |
| technical | 0300-0399 | 技术实现 | 日志、监控、CI/CD |
| governance | 0900-0999 | 治理流程 | ADR 流程、审查规范 |

#### 编号查询

```bash
# 查询各层级的下一个可用编号
./scripts/adr-cli.sh next-number constitutional  # 输出：0009
./scripts/adr-cli.sh next-number structure       # 输出：0126
./scripts/adr-cli.sh next-number runtime         # 输出：0241
```

#### 列出 ADR

```bash
# 列出所有 ADR
./scripts/adr-cli.sh list

# 列出特定层级
./scripts/adr-cli.sh list constitutional
```

---

### 4. 健康报告生成器

#### 用途

生成 ADR 治理体系的综合健康度报告，用于定期评估和改进。

#### 报告内容

1. **执行摘要**
   - 总体健康度评分
   - 关键指标概览

2. **ADR 文档统计**
   - 按层级分布
   - 按状态分布

3. **测试覆盖率**
   - 需要测试覆盖的 ADR 数量
   - 实际有测试的 ADR 数量
   - 覆盖率百分比

4. **Prompt 映射**
   - ADR 总数
   - Prompt 文件数
   - 映射率

5. **改进建议**
   - 高/中/低优先级建议
   - 具体操作步骤

#### 使用示例

```bash
# 生成到默认位置
./scripts/generate-health-report.sh

# 生成到指定文件
./scripts/generate-health-report.sh /path/to/report.md
```

#### 建议频率

| 频率 | 场景 |
|-----|------|
| 每周 | 活跃开发期，ADR 变更频繁 |
| 每月 | 常规维护期 |
| 季度 | 全面评估和规划 |

#### 报告使用

1. 查看健康度趋势
2. 识别需要改进的领域
3. 跟踪修正进度
4. 团队沟通和汇报

---

### 5. 可裁决性速查工具

#### 用途

从 ADR 中提取关键约束，生成快速参考手册，帮助开发者快速了解架构红线。

#### 提取内容

- 🔴 **红线约束**：作为强制约束条件，违反即违规
- 🟡 **建议约束**：强烈建议，可申请例外
- ✅ **架构测试**：需要测试覆盖的条款
- 🚧 **人工审核**：需要人工判断的场景

#### 使用示例

```bash
# 输出到控制台
./scripts/generate-quick-reference.sh

# 生成文件
./scripts/generate-quick-reference.sh docs/adr-quick-reference.md
```

#### 速查手册结构

```markdown
# ADR 可裁决性速查手册

## 宪法层 (constitutional)

### ADR-0001：模块化单体架构

#### 🔴 红线约束
- 模块**禁止**直接引用其他模块
- 模块**必须**通过事件/契约通信

#### ✅必须测试覆盖
- 模块隔离测试
- 跨模块引用检测
```

#### 应用场景

1. **新成员入职**：快速了解架构约束
2. **代码审查**：检查是否违反红线
3. **架构讨论**：快速查阅相关规则
4. **例外申请**：明确需要违反的约束

---

## 常见场景

### 场景 1：创建新 ADR

**步骤**：

1. 确定层级
   ```bash
   # 如果不确定，查看层级说明
   cat scripts/README.md | grep -A 10 "层级编号规范"
   ```

2. 查询可用编号
   ```bash
   ./scripts/adr-cli.sh next-number structure
   ```

3. 创建 ADR
   ```bash
   ./scripts/adr-cli.sh create structure "我的新规范"
   ```

4. 编辑文档
   - 填写规则本体
   - 定义执法模型
   - 如需测试，可添加架构测试标记

5. 创建测试（如需要）
   ```bash
   vim src/tests/ArchitectureTests/ADR/ADR_XXXX_Architecture_Tests.cs
   ```

6. 完善 Prompt
   ```bash
   vim docs/copilot/adr-XXX.prompts.md
   ```

7. 验证
   ```bash
   ./scripts/adr-cli.sh validate
   ```

---

### 场景 2：修订现有 ADR

**步骤**：

1. 修改 ADR 文档
   ```bash
   vim docs/adr/constitutional/ADR-0001-*.md
   ```

2. 同步更新测试
   ```bash
   vim src/tests/ArchitectureTests/ADR/ADR_0001_Architecture_Tests.cs
   ```

3. 同步更新 Prompt
   ```bash
   vim docs/copilot/adr-1.prompts.md
   ```

4. 运行映射验证
   ```bash
   ./scripts/validate-three-way-mapping.sh
   ```

5. 检查修正清单
   - 如有问题，根据清单逐项修复

6. 重新验证
   ```bash
   ./scripts/adr-cli.sh validate
   ```

---

### 场景 3：定期维护

**每周任务**：

```bash
# 1. 运行一致性检查
./scripts/validate-adr-consistency.sh

# 2. 运行映射验证
./scripts/validate-three-way-mapping.sh

# 3. 如有问题，记录到 Issue
```

**每月任务**：

```bash
# 1. 生成健康报告
./scripts/generate-health-report.sh

# 2. 审查报告，识别改进项

# 3. 更新速查手册
./scripts/generate-quick-reference.sh docs/adr-quick-reference.md

# 4. 团队分享报告
```

---

### 场景 4：CI 失败修复

**问题**：CI 中 ADR 验证失败

**步骤**：

1. 查看 CI 日志
   - 定位具体失败的验证步骤

2. 本地重现
   ```bash
   ./scripts/validate-adr-consistency.sh
   # 或
   ./scripts/validate-three-way-mapping.sh
   ```

3. 根据错误消息修复
   - 参考工具输出的具体建议

4. 重新验证
   ```bash
   ./scripts/adr-cli.sh validate
   ```

5. 提交修复
   ```bash
   git add .
   git commit -m "fix(adr): 修复一致性问题"
   git push
   ```

---

## 最佳实践

### 1. 使用工具创建 ADR

**推荐**：
```bash
./scripts/adr-cli.sh create structure "我的规范"
```

**不推荐**：
- 手动创建文件
- 复制粘贴现有 ADR

**原因**：
- 自动分配正确编号
- 自动填充元数据
- 自动创建 Prompt 文件
- 减少人为错误

---

### 2. 提交前验证

**推荐工作流**：

```bash
# 1. 修改代码/文档
vim docs/adr/...

# 2. 验证
./scripts/adr-cli.sh validate

# 3. 如有问题，修复后重新验证

# 4. 提交
git add .
git commit -m "..."
```

---

### 3. 定期生成报告

**建议**：

- 每月初生成上月报告
- 团队会议中分享
- 跟踪改进进度
- 记录趋势变化

---

### 4. 保持工具更新

**定期检查**：

```bash
# 查看工具版本
head -n 20 scripts/README.md | grep "版本"

# 查看更新日志
git log -- scripts/
```

---

## 故障排查

### 问题 1：脚本权限错误

**症状**：
```
bash: ./scripts/xxx.sh: Permission denied
```

**解决**：
```bash
chmod +x scripts/*.sh
```

---

### 问题 2：找不到 ADR 文件

**症状**：
```
发现 0 个 ADR 文档
```

**检查**：
```bash
# 确认路径正确
ls -la docs/adr/

# 确认文件命名正确
find docs/adr -name "ADR-*.md"
```

---

### 问题 3：编号冲突

**症状**：
```
❌ 编号冲突：ADR-0120 已存在
```

**解决**：
```bash
# 查询下一个可用编号
./scripts/adr-cli.sh next-number structure

# 使用可用编号
./scripts/adr-cli.sh create structure "标题"
```

---

### 问题 4：映射验证失败

**症状**：
```
⚠️ ADR-0001：需要测试但缺少测试文件
```

**解决**：

1. 确认是否真的需要测试
   - 查看 ADR 是否需要测试覆盖

2. 如需要，创建测试文件
   ```bash
   touch src/tests/ArchitectureTests/ADR/ADR_0001_Architecture_Tests.cs
   ```

3. 或者，移除测试标记（如该 ADR 不需要测试）

---

## 附录

### 工具依赖

所有工具仅依赖标准 Unix 工具：

- `bash` - Shell 环境
- `find` - 文件查找
- `grep` - 文本搜索
- `sed` - 文本处理
- `awk` - 文本分析

Windows 用户可使用：
- Git Bash
- WSL (Windows Subsystem for Linux)
- PowerShell 版本（`.ps1` 文件）

### 获取帮助

1. **工具帮助**
   ```bash
   ./scripts/adr-cli.sh help
   ```

2. **查看文档**
   - [scripts/README.md](./README.md)
   - [ADR 流程规范](../docs/adr/governance/ADR-900-adr-process.md)

3. **联系支持**
   - 提交 Issue
   - 咨询架构委员会

---

**版本**：1.0  
**最后更新**：2026-01-26  
**维护者**：架构委员会
